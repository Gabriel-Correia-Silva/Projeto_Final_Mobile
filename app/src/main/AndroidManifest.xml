<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".adapter.ComentariosUsuario"
            android:exported="false" />
        <activity
            android:name=".fragments.PratoMarmitaria"
            android:exported="false" />
        <activity
            android:name=".fragments.ItemMarmita"
            android:exported="false" />
        <activity
            android:name=".fragments.ConteudoMarmitaria"
            android:exported="false" />
        <activity
            android:name=".fragments.ItemLayout"
            android:exported="false" />
        <activity
            android:name=".fragments.MenuGaveta"
            android:exported="false" />
        <activity
            android:name=".FragmentMap"
            android:exported="false" />
        <activity
            android:name=".RecyclerViewFragment"
            android:exported="false"
            android:label="@string/title_activity_recycler_view_fragment"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity2"
            android:exported="false"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />

        <activity
            android:name=".activities.Cadastro"
            android:exported="false" />
        <activity
            android:name=".activities.Login"
            android:exported="false" />
        <activity android:name=".activities.MainScreen" />
        <activity android:name=".activities.AlterarDados" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>